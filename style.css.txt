/* style.css */
@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');

body {
    font-family: 'Roboto', sans-serif;
    background-color: #f0f2f5;
    color: #333;
    margin: 0;
    padding: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
}

#game-container {
    width: 100%;
    max-width: 600px;
    background-color: #ffffff;
    border-radius: 20px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    padding: 25px 35px;
    box-sizing: border-box;
    text-align: center;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
}

h1, h2 {
    color: #2c3e50;
}

#clicker-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px; /* Espacio entre elementos */
}

.income-display {
    font-size: 1.1em;
    color: #3498db;
    font-weight: 700;
}

.balance-display {
    font-size: 2.8em;
    margin: 10px 0;
}

#globe-container {
    position: relative;
    display: inline-block;
}

#globe {
    max-width: 250px; /* Tamaño del globo */
    cursor: pointer;
    user-select: none;
    transition: transform 0.1s ease-out;
}

#globe:active {
    transform: scale(0.97);
}

#click-feedback {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 32px;
    color: #ffffff;
    font-weight: bold;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.5s ease, transform 0.5s ease;
    text-shadow: 0 0 10px rgba(0,0,0,0.5);
}

#click-feedback.show {
    opacity: 1;
    transform: translate(-50%, -150%);
}

#projects-section {
    margin-top: 30px;
}

#projects-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

/* --- Nuevo y rediseñado estilo para los proyectos --- */
.project {
    background: linear-gradient(180deg, #fdfdfd, #e9e9e9);
    border: 2px solid #bdc3c7;
    border-radius: 12px;
    padding: 10px 15px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05);
}

.project-info {
    text-align: left;
    flex-grow: 1;
}

.project-info h3 {
    margin: 0 0 5px 0;
    font-size: 1.2em;
}

.project-details {
    display: flex;
    gap: 15px;
    font-size: 0.9em;
    color: #7f8c8d;
}

.buy-button {
    background: linear-gradient(180deg, #3498db, #2980b9);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 12px 18px;
    cursor: pointer;
    font-weight: bold;
    font-size: 1em;
    transition: all 0.2s;
    box-shadow: 0 3px 0 #1f648e;
}

.buy-button:hover {
    background: linear-gradient(180deg, #4aa9e9, #3990d0);
}

.buy-button:active {
    transform: translateY(2px);
    box-shadow: 0 1px 0 #1f648e;
}

.buy-button:disabled {
    background: #bdc3c7;
    cursor: not-allowed;
    box-shadow: 0 3px 0 #95a5a6;
}

footer {
    margin-top: 30px;
    font-size: 0.9em;
    color: #95a5a6;
}